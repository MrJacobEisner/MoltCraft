[project]
name = "moltcraft"
version = "0.1.0"
description = "Minecraft server with REST API for AI bot control"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.129.0",
    "httpx>=0.28.1",
    "uvicorn>=0.40.0",
]
