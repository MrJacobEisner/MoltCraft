{
  "id": "mineclaw",
  "name": "MineClaw",
  "description": "Minecraft channel plugin — chat with players and control bots via MineClaw server",
  "channels": ["mineclaw"],
  "skills": ["skills/mineclaw"],
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "enabled": { "type": "boolean" },
      "apiUrl": { "type": "string", "description": "MineClaw server API URL" },
      "apiKey": { "type": "string", "description": "MineClaw API bearer token" },
      "webhookPort": { "type": "integer", "minimum": 1024, "maximum": 65535, "default": 18790, "description": "Port for webhook listener" },
      "webhookToken": { "type": "string", "description": "Shared secret for webhook authentication" },
      "accounts": { "type": "object", "additionalProperties": true },
      "dm": {
        "type": "object",
        "properties": {
          "policy": { "type": "string", "enum": ["open", "pairing", "allowlist", "disabled"] },
          "allowFrom": { "type": "array", "items": { "type": "string" } }
        }
      }
    }
  },
  "uiHints": {
    "apiKey": { "sensitive": true },
    "webhookToken": { "sensitive": true }
  }
}
